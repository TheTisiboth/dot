# Production override for Dokploy: frisbee-bot has no Domain, so Dokploy doesn't attach it to
# dokploy-network; homepage needs it there for its health dot. Ollama stays on the private network.

services:
  frisbee-bot:
    networks:
      - default
      - dokploy-network

networks:
  dokploy-network:
    external: true
